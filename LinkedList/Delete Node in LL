import java.util.*;

class ListNode {
    int val;
    ListNode next;
    
    ListNode(int x) {
        val = x;
    }
}

class BeingZero {
    void removeElements(ListNode node) {
        node.val = node.next.val;
        node.next = node.next.next;
    }
}
